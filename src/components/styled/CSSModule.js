import React from 'react';
import styles from './CSSModule.module.css';

const CSSModule = () => {
  return (
    <div>
      <span className={styles.text}>CSS Module</span>
    </div>
  );
};

export default CSSModule;
